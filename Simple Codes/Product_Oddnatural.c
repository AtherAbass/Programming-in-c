//Program to find sum of first n odd natural numbers.
#include<stdio.h>
int main()
{
	int n, i, sum;
	sum = 0;
	printf("Enter the value of n:");
	scanf("%d", &n);
	for(i = 1; i <= n; i++)
	
}